const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

let min = 1;
let maximum = 10;

let random = Math.ceil(Math.random() * (maximum - min) + min);

Number.isInteger(random);

const num3 = 27;
const num4 = 23;
const mod = num3 % num4;

const max = Math.max(1, 2, 4, 6, 20);